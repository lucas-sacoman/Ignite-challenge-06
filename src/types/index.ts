export interface CategoryWrapData {
	id?: number;
	title: string;
	image: string;
}

export interface ContinentData {
	id?: number;
	title: string;
	description: string;
	image: string;
}

export interface CityData {
	id?: number;
	city: string;
	country: string;
	image: string;
	background: string;
}
